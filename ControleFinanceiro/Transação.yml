info:
  name: Transação
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/lucas/transactions
  body:
    type: json
    data: |
      {
        "description": "Salario do mes",
        "amount": 4500.00,
        "type": "entrada",
        "status": "recebido",
        "month": "2026-03"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
