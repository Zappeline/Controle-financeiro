info:
  name: Atualizar transação
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/lucas/transactions/1
  body:
    type: json
    data: |
      {
        "description": "Salario do mes atualizado",
        "amount": 5000.00,
        "type": "entrada",
        "status": "recebido",
        "month": "2026-03"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
